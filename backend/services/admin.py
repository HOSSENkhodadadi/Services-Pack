from django.contrib import admin

# Register your models here.
# When you create database models in models.py, register them here
# to make them accessible in the Django admin interface.

# Example:
# from .models import ChatMessage
# admin.site.register(ChatMessage)
